# Commercial Assurance — Architecture Index

**Status:** ACTIVE — a pointer document. It tells a future agent *where to read*, not *what the design is*. It duplicates nothing; every row links to an authoritative doc.
**Scope:** CRC, Living Knowledge, the CRC → Commercial Assurance handoff (CAH-4x), the Reviewer surfaces, and the Assessment Workbook / assessment authority boundary.
**Created:** 2026-09-10, during the CAH-4F documentation-contract milestone.
**As-built basis for the "shipped" column:** `origin/main` = `9fa6d1c`.

> If you are about to do reviewer-surface or CRC-handoff engineering, read the **authority boundary** (ADR-001) and the **layering invariant** (`CRC_CURRENT_STATE.md §2`) first.

---

## 1. The three authority surfaces (do not collapse)

| Surface | Authority | Product spec | Internal design | Shipped |
|---|---|---|---|---|
| **Assessment Workbook** | reviewer's evidence evaluation + human judgment (produces evidence, gaps, findings, outcome, sign-off, report) | `08_Platform/prds/PRD_ASSESSMENT_SERVICE_v1.0.md`, `08_Platform/prds/PRD_REVIEWER_WORKBOOK_UI.md` | `06_Operations/reviewer-workbook/SI8-Reviewer-Workbook-Schema-v0.1.md`, `SI8-Reviewer-Manual-v0.2.md` | pre-CAH-4x |
| **Reviewer Living Knowledge** | governed research — `Adopted`, reviewer-scoped `TopicClaim`s + deterministic applicability; never a judgment, control result, finding, or legal conclusion | `08_Platform/prds/PRD_CAH_4F_REVIEWER_RESOURCES.md` | `08_Platform/implementation/REVIEWER_RESOURCES_ARCHITECTURE.md` | CAH-4E (`9fa6d1c`); Reviewer Resources inspector = CAH-4F.1 (`b0e067f`, prod-deployed); rail time-sharing = CAH-4F.2 (`f29b0fb`, CLOSED / production-proven 2026-09-10) |
| **Linked CRC Context** | customer/project context from an *educational* CRC conversation — context, not assessment evidence | `08_Platform/prds/PRD_CRC_v1.0.md` §§2,12,14 | `08_Platform/implementation/REVIEWER_RESOURCES_ARCHITECTURE.md` §1.2, §5.3 | CAH-4B / CAH-4C; always-a-tab + neutral empty state = CAH-4F.1 (`b0e067f`, prod-deployed); rail time-sharing = CAH-4F.2 (`f29b0fb`, CLOSED / production-proven 2026-09-10) |

**The decision that binds them:** `08_Platform/app/lib/reviewer-lk/ADR-001-reviewer-resources-authority-boundary.md`.

## 2. Where to read about each topic

| Topic | Authoritative doc(s) | Key files |
|---|---|---|
| **CRC — product boundary & current engineering status** | `08_Platform/implementation/CRC_CURRENT_STATE.md` (rolling status), `08_Platform/prds/PRD_CRC_v1.0.md` (frozen spec), `08_Platform/implementation/CRC_IMPLEMENTATION_RISKS.md` | `lib/crc-engine/**`, `lib/interview-engine/**` |
| **Living Knowledge — governance model** | `06_Operations/institutional-knowledge/notebook/GOVERNED-CLAIMS.md` (canonical claims), `PLATFORM-RIGHTS-MATRIX.md` (tool-scoped claims), `CRC-PUBLICATION-POLICY.md`, `PRD_LIVING_NOTEBOOK.md`, `PRD_LIVING_KNOWLEDGE_SOURCE_INPUTS_v0.1.md` | `lib/retrieval-engine/topic-claims-fixture.ts`, `matrix-fixture.ts` |
| **Living Knowledge → Retrieval → Bounded Interpretation → Projection (the layering invariant)** | `08_Platform/implementation/RETRIEVAL_ENGINE_ARCHITECTURE.md`, `PROJECTION_LAYER_ARCHITECTURE.md`, `LK_PHASE1_TECHNICAL_DESIGN.md` / `_v2.md`, `CRC_CURRENT_STATE.md §2` | `lib/retrieval-engine/**`, `lib/bounded-interpretation/**`, `lib/projection-layer/**` |
| **Applicability evaluation (deterministic)** | `RETRIEVAL_ENGINE_ARCHITECTURE.md`, `THIRD_PARTY_SOURCE_ASSETS_ROUTING_ARCHITECTURE.md`, `THIRD_PARTY_SOURCE_RIGHTS_PATH_A_PROVIDER_NARROWING.md` | `lib/retrieval-engine/lookup-topic-claims.ts` (`evaluateApplicabilityDetailed`, `providerScopeMatches`, `toolScopeMatches`) |
| **CRC-active identity / canonicalization reachability** | `CRC_CURRENT_STATE.md §9`, LK-93/LK-94 notes therein | `lib/crc-engine/canonicalization-readiness.ts`, `lib/crc-engine/crc-active-reachability-backstop.ts`, `lib/interview-engine/extraction.ts` (`KNOWN_TOOLS`) |
| **CRC → Commercial Assurance linkage (customer explicitly links a CRC conversation to their submission)** | `REVIEWER_RESOURCES_ARCHITECTURE.md §1.2`; the milestone codes are CAH-3B/3D/3E/3F | `lib/crc-assurance-handoff/**` (`capabilities/email-correlation.ts`, `state-binding.ts`, `service.ts`, `repository.ts`), `crc_assurance_associations` + `crc_assurance_association_events` (migration `20260904000000`), `app/api/dashboard/submissions/[id]/crc-association/route.ts` |
| **Reviewer CRC Context (admin sees linked context separately from the workbook)** | `REVIEWER_RESOURCES_ARCHITECTURE.md §1.2, §5.3` (CAH-4B) | `lib/reviewer-context/**`, `lib/crc-project-context/**`, `app/api/admin/submissions/[id]/reviewer-crc-context/route.ts`, `ReviewerCrcContextPanel.tsx` |
| **Reviewer CRC transcript access (deliberate, scoped, audited)** | `REVIEWER_RESOURCES_ARCHITECTURE.md §1.2, §6` (CAH-4C) | `app/api/admin/submissions/[id]/reviewer-crc-context/[association_id]/transcript/route.ts`, `ReviewerTranscriptDrawer.tsx`, migration `20260909010000_reviewer_crc_context_access_events.sql` |
| **Reviewer Living Knowledge (CAH-4E) + Reviewer Resources (CAH-4F) + HRR converged interface (CAH-4G)** | `PRD_CAH_4F_REVIEWER_RESOURCES.md`, `PRD_CAH_4G_HRR.md`, `REVIEWER_RESOURCES_ARCHITECTURE.md` (§18 HRR UAT script), `ADR-001` / `ADR-002` | `lib/reviewer-lk/**`, `lib/hrr/**`, `lib/hrr-audit/**`, `app/api/admin/submissions/[id]/reviewer-lk/research/route.ts` (**the one HRR route** — the legacy `reviewer-lk/route.ts` GET was RETIRED CAH-4G.7), `ReviewerLkPanel.tsx` / `ReviewerLkLookup.tsx` / `HrrResearchAnswerView.tsx`, migration `20260910000000_reviewer_lk_research_access_kind.sql` (no CAH-4G migration) |
| **HRR (Human Reviewer Research) + GRI (Governed Research Interface) — CAH-4G** | `PRD_CAH_4G_HRR.md` (the *what*), `HRR_GRI_TECHNICAL_DESIGN.md` (the *how*), `ADR-002-governed-research-interface.md` (durable decision: a natural-language research interface is intent entry into governed retrieval + Bounded Interpretation, not an answer authority) | **`CAH-4G.10 SLICE A — CLOSED / PRODUCTION-PROVEN / CAH-4G CONVERSATIONAL HRR — VISIBLE THREAD PRODUCTION-PROVEN, BOUNDED CONTEXT NOT STARTED / CAH-4G.11 — FOLLOW-UP DISCOVERY, DESIGN / EVIDENCE COLLECTION / CAH-4G.12 — TOPIC-SCOPED HRR RESEARCH SESSION ARCHITECTURE, DESIGN, ACCEPTED / CAH-4G.13 — SESSION-FOCUSED PRODUCTION DISCOVERY, PRE-REGISTERED, PM EXECUTION PENDING`** (`HRR_RESEARCH_SESSION_CONTRACT.md` — PM: HRR is a bounded, topic-anchored Research Session, not unlimited workbook-wide conversation; not implemented) — **Slices 1–7 + CAH-4G.7P + CAH-4G.8 + CAH-4G.9 design + CAH-4G.10 Slice A + CAH-4G.10P reconciliation + CAH-4G.10I integration (2026-09-10 / -11)**. **CAH-4G.10P** proved a **DEPLOYMENT GAP** (Slice A reviewed locally but on no origin ref; a UAT correctly observed the pre-Slice-A surface). **CAH-4G.10I (2026-09-11) — resolved:** the reviewed lineage was rebased onto `origin/main` (`d06c668`; 2 zero-overlap CRC drift commits), `git range-diff` all `=`, then **fast-forward-pushed `d06c668 → 4b9ee5a`** (no force; `origin/main` confirmed `== 4b9ee5a`); `tsc` clean, `next build` exit 0, zero new test failures vs a fresh `d06c668` baseline. Vercel auto-deploy observed (SHA not independently certified from this environment at the time). **CAH-4G.10C (2026-09-11):** a PM-performed authenticated production UAT against `CA-RLK-2a PROD SMOKE` / `ASSESS-007-2026-09-07` passed all 10 acceptance checks — evidence: `HRR_CONVERSATIONAL_ARCHITECTURE.md §FF`. **CAH-4G.10 Slice A — CLOSED / PRODUCTION-PROVEN (the VISIBLE thread only). Slice B remains NOT STARTED / NOT AUTHORIZED.** **CAH-4G.10 Slice A (integrated + production-proven):** the VISIBLE conversational thread — `app/admin/submissions/[id]/review/hrr-thread.ts` (a local pure append-only reducer; LOCAL HRR mechanics, no generic framework) + `ReviewerLkLookup` rewritten to `useReducer`. Successive research turns stack; a second question no longer replaces the first. VISIBLE only — no `prior_context`, no prior turn to the classifier / retrieval / applicability / BI / composition / audit; request body byte-unchanged; topic 0 / free-form ≤1 model calls; "Clear conversation" is client-only. `tsc` clean, `next build` exit 0, full-suite failing set byte-identical to a fresh `08f20e9` baseline (zero new failures). As-built: `HRR_CONVERSATIONAL_ARCHITECTURE.md §BB`. **CAH-4G.9 (design):** append-only client-only thread; bounded enum-only follow-up referent (`{topic, scope, bi_status}`), never transcript-to-LLM; one-model-call ceiling; no persistence / audit / schema / CRC change; CRC's `buildUserMessageContent` is the precedent. GRI formalised as a *pattern*. Slice B (bounded follow-up referent) + Slice C not started. CAH-4G.8: series re-rebased onto the advanced `origin/main` (`9219496`, +5 NY §396-b Living Knowledge governance commits — clean; `likeness` gained a NY-jurisdiction-scoped reviewer-eligible claim), `git range-diff` all `=`, zero new test failures vs a fresh `9219496` baseline, `tsc` clean, `next build` exit 0, **fast-forward-merged to `main` `9219496 → 0413291` (no force)** and deployed to `app.superimmersive8.com` (route topology confirms: `POST .../reviewer-lk/research` → `401`, legacy `GET` → `404`). NOT yet `PRODUCTION-PROVEN` — the authenticated production browser UAT + live classifier eval + production audit-DB proof were not run (no connected browser / admin credential / `ANTHROPIC_API_KEY` / audit-DB access in the session). **Slice 1:** `BiIntent` + `userGoalsToBiIntents`; mixed-intent authority contract corrected. **Slice 2:** `lib/reviewer-lk/interpret-research-intent.*` (one classify-only Anthropic call, enum-only schema, fails closed & never throws) + `hrr-authority-gate.ts` (deterministic). **Slice 3:** NEW `lib/hrr/` — `runHrrResearch()` (PURE; one downstream path for the topic chip and the free-form classifier+gate) + `researchIntentToBiIntent` / `reviewerClaimToBiResult` adapters + `HrrResearchResult`. `BiResult` introduced with **zero CRC churn** (`RetrievalResult` structurally satisfies it), proven byte-for-byte behavior-neutral; reviewer selection/eligibility/applicability byte-unchanged; `not_met` claims excluded from BI (`does_not_apply[]`); raw reviewer question never enters selection/applicability/BI. **Slice 3A:** deterministic applicability is a generic upstream semantic input to BI (`BiResult.applicability`, optional — CRC byte-unchanged) → an applicability-`unresolved` governed proposition is interpreted `relevant_applicability_unresolved`, **never `directly_relevant`** (proposition still visible; unresolved requirement preserved; no new BI status; no composition workaround; durable principle in `ADR-002` / `HRR_GRI_TECHNICAL_DESIGN.md §H`). **Slice 4:** `lib/hrr/project-hrr-research-answer.ts` — `projectHrrResearchAnswer(HrrResearchResult) → HrrResearchAnswer`, **deterministic, no model call**. Consultant-like answer: fixed `orientation` per BI status, verbatim governed propositions + verbatim `summary_blocks`, mechanical deduplicated applicability rollup, `unresolved_inputs` made useful (each assigned to the Human Reviewer), separate `does_not_apply` block, fixed `boundary_note`. Authored prose = one enumerable constant (`HRR_ANSWER_TEMPLATES`); grounding A–E test-enforced; two structurally-separate lanes; `relevant_applicability_unresolved` wording-locked; ordering is deterministic input order (documented). No `rules.ts` / CRC-composition change. **Slice 5:** `lib/hrr/project-hrr-audit-record.ts` (PURE — `projectHrrAuditRecord` → `{actorUserId, submissionId}` or `null`) + `lib/hrr-audit/run-audited-hrr-research.ts` (`runAuditedHrrResearch` — the one impure boundary: pure pipeline → audit → answer; `HrrAuditNotRecordedError` on audit-write failure → route's safe 503, zero content). **Reuses the CAH-4E `lk_research` access contract UNCHANGED — NO migration** (Option A; source inspection superseded the frozen Option B — the deployed row records no topic/claim, and the frozen Option-B placement breaks `authority-firewall.test.ts`). Audit ≠ evidence; authority-only/unsupported → no event; multi-topic/mixed → one row per action; raw question never persisted / hashed. **Slice 6 (CAH-4G.6):** first user-facing integration. `POST .../reviewer-lk/research` (both entry modes → `runAuditedHrrResearch`; strict input contract — `messages[]`/`history`/`session_id` → 400; length-bounded; `topic_pick` = 0 model calls, `question` = 1 classify-only, fails closed; audit-throw → 503 zero content; actor never from body) + ONE renderer `HrrResearchAnswerView` (no topic/question split, no topic-specific branch; orientation prominent; unresolved before provenance; assessment-authority boundary a distinct callout; empty sections omitted; neutral styling; no `crc_eligible`/`crc_publication_scope`; no promotion control) + rewritten `ReviewerLkLookup` (topic chips run research on click; `<textarea>` Ask HRR; ONE current response; race-guarded; raw question never persisted / URL / logged; page reload clears it). CAH-4F.1/4F.2 shell + inspector + Linked-CRC tab separation + responsive behaviour unchanged. **Slice 7 (CAH-4G.7):** server-rendered-markup composition-quality review (no authenticated browser path in the build env) → renderer-only consultative hardening (`HrrResearchAnswerView` — "what remains unresolved" leads; two provenance disclosures → one; per-claim requirement/ref re-lists dropped; "Not yet established" rollup group dropped) — no BI / composition / contract change; the dead legacy `GET .../reviewer-lk` route + `route-and-audit.test.ts` + `projectReviewerLkResult` + `ReviewerLkLookupResult` **RETIRED** (fail-closed audit contract transferred to `hrr-research-route.test.ts`); `HRR_MAX_RESOLVED_TOPICS` kept at 2; live classifier eval + authenticated visual UAT **pending a credentialed environment**. **Not deployed; status `IMPLEMENTED / INTEGRATION PENDING`, not `PRODUCTION-PROVEN`.** Zero new test failures across all seven slices; `tsc` clean; `next build` exit 0. No CRC behavior change. |
| **Reviewer-side access audit** | `REVIEWER_RESOURCES_ARCHITECTURE.md §6`; HRR reuse: `HRR_GRI_TECHNICAL_DESIGN.md §K` | `crc_context_access_events` (`access_kind ∈ {transcript, lk_research}`), `lib/reviewer-lk/repository.ts` (`recordReviewerLkAccess` — the one writer), `lib/reviewer-context/repository.ts`. **CAH-4G.5:** HRR reuses the `lk_research` contract UNCHANGED (no migration) via the PURE `lib/hrr/project-hrr-audit-record.ts` + the impure `lib/hrr-audit/run-audited-hrr-research.ts` (fail-closed audit-before-content); authority-only/unsupported → no event; one row per reviewer action; raw question never persisted. |
| **Assessment Registry / lifecycle / provenance / publication** | `PRD_ASSESSMENT_SERVICE_v1.0.md`, `ADR-001..004` in `lib/assessments/` | `lib/assessments/**`, `assessments` + `assessment_publications` tables |
| **Workbook / assessment authority boundary vs. Reviewer Resources** | `ADR-001-reviewer-resources-authority-boundary.md`, `PRD_CAH_4F_REVIEWER_RESOURCES.md §4` | `app/admin/submissions/[id]/review/page.tsx`, `WorkbookClient.tsx`, `Section1Intake.tsx`…`Section7Brief.tsx`, `workbook-schema.ts` |
| **Customer / operational journey (end-to-end)** | `06_Operations/customer-experience/SI8-Customer-Journey-v1.0.md`, `SI8-Operational-Journey-v1.0.md` | — |
| **Submission fact acquisition — upstream of Consultative Composition (CAH-4I, discovery only, not yet actioned)** | `08_Platform/implementation/COMMERCIAL_ASSURANCE_SUBMISSION_FACT_COVERAGE_DISCOVERY.md` — whether CertForm/RecordForm collect the project facts the current Commercial Assurance architecture (workbook, CRC, LK) actually needs; coverage matrix; territory/registration/trademark findings; no implementation authorized | `app/certify/page.tsx`, `app/record/page.tsx`, `lib/reviewer-lk/submission-facts.ts`, `lib/crc-engine/dependency-askability.ts` |

## 3. Milestone map (CAH-4x)

| Milestone | What it shipped | Status |
|---|---|---|
| CAH-4A | reviewer-context + LK consumer contract — architecture design | design only |
| CAH-4B | reviewer CRC context read path + authority firewall | shipped, in `9fa6d1c` |
| CAH-4C | reviewer CRC transcript access + fail-closed audit (`access_kind:'transcript'`) | shipped, in `9fa6d1c` |
| CAH-4D | reviewer Living Knowledge architecture inventory | design only |
| CAH-4E | Human Reviewer Living Knowledge V1 (reviewer-eligible `publication_scope` gate, `access_kind:'lk_research'`, look-up route + panel) | shipped + integrated (`9fa6d1c`); production application of `20260910000000` and the CAH-4E production smoke are asserted by the PM checkpoint |
| **CAH-4F** | **Reviewer Resources concept + LK Context Semantics** — proposition-first card, explicit Applicability heading, plain-English topic labels, `crc_publication_scope` prose + `crc_eligible` removed. Shipped as a **grouped top-of-page block**. | **integrated + deployed (`origin/main` = `ed3333e`, 2026-09-09).** Semantic work validated + retained; the top-of-page *layout* did not deliver the primary/secondary hierarchy and is superseded by CAH-4F.1. |
| **CAH-4F.1** | **Reviewer Workspace Shell** — generic client `ReviewerShell` owns the page frame; `ASSESSMENT NAVIGATION \| WORK SURFACE \| REVIEWER RESOURCES` inspector (closable; adjacent column ≥1440px / overlay drawer below; tabs: Living Knowledge, Linked CRC Context). All CAH-4F semantics moved into the inspector. `WorkbookClient` change = one line (`h-screen`→`h-full`). No route/selector/eligibility/applicability/audit/schema/migration change. | **INTEGRATED / PRODUCTION DEPLOYED — VISUAL UAT PENDING** (2026-09-10). `origin/main` = `b0e067f` (rebased onto `9203162`, fast-forwarded; feat `f76dc85`); Vercel `si8-creator-portal` Production build `success` on `app.superimmersive8.com`. Manual visual UAT (§16) not yet performed — not production-proven. `REVIEWER_RESOURCES_ARCHITECTURE.md` §1/§7/§11/§16. |
| **CAH-4F.2** | **Contextual Inspector Coordination** — production visual UAT of CAH-4F.1 confirmed the shell but exposed four-region density at ~1440px. Contextual right rail now holds **one surface at a time at adjacent widths**: Reviewer Resources CLOSED → `NAV \| WORKBOOK \| WORKBOOK CONTEXT`; OPEN → `NAV \| WORKBOOK \| REVIEWER RESOURCES`. `ReviewerShell` derives one **layout-only** boolean (`workbookContextAsideHidden = resourcesAvailable && inspectorOpen && adjacent`), published via the domain-neutral `workspace-layout-context` around the opaque Workbook child; `WorkbookClient` CSS-hides (never unmounts) its own Guidance/Submission/Evidence aside; `rightTab` stays Workbook-owned. UX mechanism, **not** an authority boundary — data/service/audit/interpretation/mutation boundaries unchanged. Uniform mutual exclusion incl. 1920px; drawer behaviour unchanged below the breakpoint. New file `workspace-layout-context.tsx`; `ReviewerShell.tsx` + `WorkbookClient.tsx` minimal edits; `page.tsx` structurally unchanged; no Reviewer Resources component touched. | **CLOSED / PRODUCTION-PROVEN** (2026-09-10). `origin/main` = `f29b0fb` (feat `4f1969d`; rebased onto `74a8f63` — Envato/Epidemic activation, zero overlap, identical patch — and fast-forwarded), then docs status commits. Vercel `si8-creator-portal` Production build `success` on `app.superimmersive8.com` (deployment `6363990353`). **Engineering proof:** 15 reviewer suites / 320 tests, `tsc` clean, `next build` exit 0, full-suite failing set byte-identical to a fresh `74a8f63` baseline, zero new failures (twice); `git diff` = layout-only, no route/schema/migration/retrieval/applicability/audit/CRC change, no Reviewer Resources component touched. **Production visual proof** (PM/user authenticated UAT, `CA-RLK-2a PROD SMOKE` / `ASSESS-007-2026-09-07`, ~1440px): closed→open→closed rail states behaved as designed (context aside CSS-yields the rail, Workbook reclaims width, prior Guidance tab preserved on close, no stacked layout, four-region density gone); one explicit `Copyright ownership` LK look-up rendered a single governed result inside the coordinated inspector (proposition primary, "Applicability" heading, context, limitations, provenance) with no `crc_eligible` / CRC-prose / evidence-promotion affordance and the Workbook still primary. **Audit path unchanged** by diff + tests — CAH-4E already production-proved the `lk_research` contract; no new audit query this session and none claimed. `REVIEWER_RESOURCES_ARCHITECTURE.md` §1/§11a/§17/§17a. |
| **CAH-4G** | **HRR V1 / Governed Research Interface (GRI)** — free-form reviewer research questions alongside the existing topic look-up, inside the same inspector. Topic path unchanged (0 model calls). Free-form path = 1 bounded structured **classify-only** model call → governed retrieval (reuses `selectReviewerClaims`) → deterministic applicability → Bounded Interpretation (`determination_declined` handles assessment-authority questions) → deterministic consultative composition → provenance. Append-only VISIBLE research thread — **integrated to `main` (`4b9ee5a`), Vercel-deployed, and authenticated-production-UAT-proven as of CAH-4G.10C** (resolving the CAH-4G.10P deployment gap; `HRR_CONVERSATIONAL_ARCHITECTURE.md §FF`); each free-form question still classified independently from its own text — no `prior_context` until Slice B; no auto CRC context; no synthetic `UserGoal`s; no invented "closest topic"; a mixed utterance's intents are routed independently (one prohibited intent never rewrites another's scope); reuse `access_kind='lk_research'`. **No CRC change.** **CAH-4G.9 design + CAH-4G.10 Slice A:** a client-only conversational research thread (`hrr-thread.ts` local reducer) + (Slice B, not started) a bounded enum-only follow-up referent — never transcript-to-LLM (`HRR_CONVERSATIONAL_ARCHITECTURE.md §BB`, `ADR-003`). | **`CAH-4G.10 SLICE A — CLOSED / PRODUCTION-PROVEN / CAH-4G CONVERSATIONAL HRR — VISIBLE THREAD PRODUCTION-PROVEN, BOUNDED CONTEXT NOT STARTED / CAH-4G.11 — FOLLOW-UP DISCOVERY, DESIGN / EVIDENCE COLLECTION / CAH-4G.12 — TOPIC-SCOPED HRR RESEARCH SESSION ARCHITECTURE, DESIGN, ACCEPTED / CAH-4G.13 — SESSION-FOCUSED PRODUCTION DISCOVERY, PRE-REGISTERED, PM EXECUTION PENDING`** (`HRR_RESEARCH_SESSION_CONTRACT.md`) — **Slices 1–6 COMPLETE (CAH-4G.1/.2/.3+3A/.4/.5/.6, 2026-09-10).** Slices 1–3A: BI adaptation + classifier + converged `runHrrResearch()` + applicability-`unresolved` → `relevant_applicability_unresolved`. Slice 4: `projectHrrResearchAnswer()` — deterministic, no model call; `HRR_ANSWER_TEMPLATES`; grounding A–E; two lanes; wording-locked. Slice 5: `runAuditedHrrResearch()` — audit-before-content, fail-closed; **reuses `lk_research` UNCHANGED, no migration** (Option A). **Slice 6:** `POST .../reviewer-lk/research` (both entry modes → the audited boundary; strict input contract; `topic_pick` = 0 model calls; audit-throw → 503 zero content; actor never client-supplied) + ONE `HrrResearchAnswerView` (no topic/question split, no topic-specific branch, neutral, progressive provenance, no promotion control) + rewritten `ReviewerLkLookup` (chips run research on click; `<textarea>` Ask HRR; ONE current response; raw question never persisted). Shell/inspector/Linked-CRC separation unchanged. **Slice 7:** renderer-only consultative hardening + legacy `GET .../reviewer-lk` route RETIRED (fail-closed audit contract transferred to `hrr-research-route.test.ts`) + `HRR_MAX_RESOLVED_TOPICS` kept at 2. **CAH-4G.7P + CAH-4G.8:** re-rebased onto `origin/main`=`9219496`, fast-forward-merged to `main` (`0413291`, no force), production-deployed to `app.superimmersive8.com`. **CAH-4G.9 (design) + CAH-4G.10 (Slice A) + CAH-4G.10P (reconciliation: DEPLOYMENT GAP found) + CAH-4G.10I (integration + deployment, 2026-09-11):** the append-only VISIBLE research thread (`ReviewerLkLookup` / `hrr-thread.ts`) was rebased onto `origin/main` (`d06c668`, zero-overlap CRC drift; `git range-diff` all `=`) and **fast-forward-pushed `d06c668 → 4b9ee5a`** (no force); `origin/main` confirmed `== 4b9ee5a`. `tsc` clean, `next build` exit 0, zero new test failures vs a fresh `d06c668` baseline. Vercel auto-deploy observed. **CAH-4G.10C (2026-09-11):** a PM-performed authenticated production UAT against `CA-RLK-2a PROD SMOKE` / `ASSESS-007-2026-09-07` PASSED all 10 acceptance checks (topic + 2 successive free-form questions all remained visible together; composer cleared each time; tab-switch + inspector close/reopen preserved the thread; workbook navigation cleared it per the V1 contract, not a defect; "Clear conversation" reset the thread with no assessment-state change) — evidence: `HRR_CONVERSATIONAL_ARCHITECTURE.md §FF`. VISIBLE only — no `prior_context`, no reasoning-context change, request body byte-unchanged, model-call budget unchanged, audit unchanged, no persistence, no CRC change. Slice B + Slice C **NOT STARTED / NOT AUTHORIZED**. **Status: `CAH-4G.10 SLICE A — CLOSED / PRODUCTION-PROVEN`; `CAH-4G CONVERSATIONAL HRR — VISIBLE THREAD PRODUCTION-PROVEN / BOUNDED CONVERSATIONAL CONTEXT NOT STARTED`; `CAH-4G.11 — FOLLOW-UP DISCOVERY / ARCHITECTURE VALIDATION — DESIGN / EVIDENCE COLLECTION` (`HRR_FOLLOWUP_DISCOVERY.md`); `CAH-4G.12 — TOPIC-SCOPED HRR RESEARCH SESSION ARCHITECTURE / DESIGN` (`HRR_RESEARCH_SESSION_CONTRACT.md`, no runtime change, Slice B still NOT STARTED / NOT AUTHORIZED).** Spec: `PRD_CAH_4G_HRR.md`; design + contract: `HRR_GRI_TECHNICAL_DESIGN.md §S–§V` + `HRR_CONVERSATIONAL_ARCHITECTURE.md`; decisions: `ADR-002` + `ADR-003-hrr-conversation-context.md`. |

## 4. Invariants a reviewer-surface change must not break

(Full list: `ADR-001` "What is currently enforced" + `CRC_CURRENT_STATE.md`.)

1. No downstream layer creates a stronger conclusion than Bounded Interpretation permits.
2. Reviewer LK eligibility = `lifecycle` + `publication_scope` + supersession; `crc_eligible` is decoupled.
3. Unresolved applicability ≠ negative finding ≠ withheld.
4. Explicit vs discovered relevance; Track A / B / C provenance; `provider_scope`; `applicability_requirements`; Lifecycle; CRC eligibility; supersession; evidence boundaries; fail-closed behaviour; correction semantics; structured-output reliability; bounded questioning; no fabricated legal conclusions.
5. Reviewer LK access is explicit-action only and audited **before** governed content is returned.
6. Reviewer Resources never mutate workbook / assessment state; visibility beside the workbook ≠ merged authority.
7. CRC context is context, not assessment evidence. Reviewer LK is governed research, not assessment judgment. The Human Reviewer owns evidence evaluation and the conclusion.

---

**Maintenance:** add a row when a new reviewer-surface subsystem or CAH milestone ships. Keep it a pointer — never copy design content here.
