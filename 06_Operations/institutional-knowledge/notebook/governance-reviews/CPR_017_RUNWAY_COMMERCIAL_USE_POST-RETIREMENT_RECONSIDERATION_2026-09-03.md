Title: CRC Publication Review #17 — Runway Commercial-Use Grant (post-retirement reconsideration)

Reviewed object:
- `CLAIM-RUNWAY-COMMERCIAL-USE-001-v1`

Review date: 2026-09-03

Artifact type: CRC Publication Review / Decision Analysis — post-retirement reconsideration. Distinct from `CPR_016` (the initial publication review, disposition WITHHOLD, sole blocker = live Matrix coexistence). `CPR_016` is preserved unmodified as the historical record of that pre-retirement finding; this artifact records an independent, later reconsideration against the now-actually-retired Matrix state, mirroring the Kling `CPR_014`→`CPR_015` precedent exactly (retirement does not automatically convert a prior WITHHOLD into APPROVE — this review independently rechecks every publication condition, not merely the coexistence blocker).

PM decision: **[PENDING — this artifact records a reviewing-agent recommendation; PM/JD concurrence, if any, is a separate, later step, not performed by this task, mirroring the CPR_015/CPR_016 precedent of leaving PM decision PENDING at initial artifact-writing time].**

Historical status: VERBATIM ARCHIVE — DO NOT EDIT HISTORICAL BODY once PM's decision is recorded. Future amendments should be appended outside the body below, or captured in a new review artifact — never inserted into the verbatim body.

Source: written directly to this file as the review was conducted this session. The evidence-freshness check in §2 was performed via a live, independent `WebFetch` of `runway.com/terms-of-use` (a third independent fetch of this document today, after `FGR_015`'s and `CPR_016`'s own separate fetches). The empirical canary in §5 was executed live, via a raw `retrieve()`/`buildBoundedInterpretations()` call against the real, now-retired Matrix state, run through a throwaway, never-committed script (`08_Platform/app/lib/crc-engine/eval/runway-cpr017-postretirement-canary-throwaway.ts`, deleted immediately after this review captured its output), mirroring the ephemeral-probe discipline used for every prior CPR in this line.

--- BEGIN VERBATIM CRC PUBLICATION REVIEW ---

# Runway Commercial-Use Grant — CRC Publication Review (Post-Retirement Reconsideration)

## 0. Repository / governance state verified before review

LK worktree (`work/lk-eu-ai-act-art50-research`) confirmed clean, HEAD `1c513c334f35086dc25e5bd34ba45be05133cb68` (the Runway Matrix retirement commit) at review start. `origin/main` confirmed at `3043522d6a885811a5ebb3f6a20b415592f819fa`, diverged 4 ahead / 3 behind (not reconciled, per this task's own scope). `origin/main`'s three additional commits — the "CAH-3B" feature (`53f49f9`, `ba9b7fd`, already independently confirmed irrelevant by both `FGR_015` and `CPR_016`) plus a new one, `3043522` ("CRC: restore applicability-readiness tool-scope parity" — a bug fix to `deriveApplicabilityReadinessGaps` restoring `activeToolIds` parity with `retrieve()`'s own TopicClaim stage, so that a `tool_scope`-narrowed claim carrying an *unresolved applicability requirement* is no longer silently dropped from readiness-gap detection before evaluation). Read directly: this fix operates entirely on claims that have a non-empty `applicability_requirements` array requiring a governed selector need to be derived; `CLAIM-RUNWAY-COMMERCIAL-USE-001-v1` has `applicability_requirements: []`, so `deriveMatrixApplicabilityGaps`/`deriveApplicabilityReadinessGaps` would never have produced a diagnostic for it before or after this fix — there is nothing for a tool-scope-parity readiness-gap fix to affect on a claim with no applicability gate. **Confirmed materially irrelevant to this specific claim, by direct code inspection, not assumed.** No STOP triggered on this basis.

Authoritative material read directly this review: `FGR_015` and `CPR_016` in full, the `GOVERNED-CLAIMS.md` Runway entry (confirmed `Lifecycle: Adopted`, `CRC Publication Scope: PENDING`, `CRC Candidate Statement: [DRAFT]`), `PLATFORM-RIGHTS-MATRIX.md`'s and `matrix-fixture.ts`'s `runway-gen3` entries (confirmed `crc_eligible: 'No'`, retirement annotation present, historical proposition/evidence/provenance text unmodified), and `topic-claims-fixture.ts` (confirmed zero Runway entries — no second CRC-active Runway representation exists anywhere). Starting state confirmed exactly: **Matrix OFF / successor WITHHELD**, matching the intended post-retirement intermediate state exactly.

## 1. Governance history reconstructed

Five distinct, non-substitutable stages confirmed present in order: (1) `FGR_015` — Formal Governance Review, Adoption decision (ADOPT, PM: JD, 2026-09-03); (2) `CPR_016` — initial CRC Publication Review, disposition WITHHOLD, sole blocker = live Matrix coexistence, empirically demonstrated via a synthetic canary showing not mere duplication but a scope *contradiction* on Enterprise (Matrix implied inclusion, successor correctly excludes it), occurring unconditionally in every reachable state since neither representation carries an applicability gate; (3) explicit JD/PM Matrix-retirement authorization (verbatim decision statement, 2026-09-03, classifying the action as representation supersession, not substantive reversal); (4) retirement execution (`1c513c33`) — `runway-gen3` `crc_eligible: 'Yes'` → `'No'` in both Matrix files, historical proposition/evidence/provenance preserved verbatim, dated annotation added; (5) this reconsideration. No earlier decision substitutes for this one — in particular, `FGR_015`'s ADOPT is an Adoption-layer decision, unaffected and unchanged by anything in the retirement/publication sequence; `CPR_016`'s WITHHOLD remains the accurate historical record of the pre-retirement state, not retroactively altered.

## 2. Evidence freshness — independently re-checked a third time, not inherited

Per `CRC-PUBLICATION-POLICY.md` Principle 1 and the established practice (already applied twice for this same claim, by `FGR_015` and `CPR_016` respectively, each performing its own independent fetch rather than reusing a prior one) of never silently extending an earlier freshness finding to a later, higher-stakes decision: this review performed its own, third, independent `WebFetch` of `runway.com/terms-of-use` (2026-09-03). Findings, confirmed fresh and unchanged: stated last-updated date **May 11, 2026**, unchanged; §4.4 non-ownership and non-restriction-of-commercial-use language confirmed verbatim ("the Company does not restrict your commercial use of your Outputs"); §5 competitive-products restriction confirmed verbatim; Enterprise carve-out independently reconfirmed verbatim ("If your organization would like to use, or has signed up for, Runway Enterprise Services, check out the Runway Enterprise Services Terms..."); no tier-based differentiation of commercial-use rights found anywhere in the document. **Classification: CURRENT.** Evidence freshness does not warrant WITHHOLD or ESCALATE at this reconsideration either.

## 3. Claim semantics — independently re-verified, nothing strengthened

Re-read directly from `GOVERNED-CLAIMS.md` and `matrix-fixture.ts`'s retirement annotation (not from `CPR_016`'s own restatement): proposition unchanged from `FGR_015`/`CPR_016` — commercial use permitted across Free/Standard/Pro/Max, Enterprise explicitly excluded on evidence grounds. `provider_scope: null`, `tool_scope: ['runway-gen3']` (existing `CANONICAL_TOOL_ID`, unchanged). `applicability_requirements: []` re-verified correct on its own evidentiary merits for a third time — the primary ToS was checked again in §2 for any per-tier or per-account gate, and none was found; this is a genuine absence, not an oversight, and not conformed to Kling's shape merely for consistency. `unresolved_project_dependencies: []`, unchanged. `Lifecycle: Adopted`, unchanged by the retirement sequence. Evidence limitations (Enterprise out of scope; the "similar or competitive products or services" restriction requires case-specific judgment; the third-party Enterprise-training-opt-out claim remains unconfirmed) preserved in Prohibited Conclusions, unaltered. Provenance (exact source URL + fetch date) intact. **No proposition strengthening found anywhere in the retirement sequence** — the retirement commit's own diff was independently re-inspected and confirmed to touch only the Matrix files' `crc_eligible` value and add a dated annotation, never `GOVERNED-CLAIMS.md`.

## 4. CRC eligibility / governance readiness — comprehensive check, not limited to the removed blocker

Evaluated independently rather than assuming clearing `CPR_016`'s sole named blocker means no other blocker could exist: Lifecycle (Adopted — sufficient), evidence classification (Class A, Official platform authority, directly fetched three separate times across three separate reviews — strong), provider scope (`null`, correctly scoped, no broadening), applicability (none, genuinely absent from evidence), dependencies (none), evidence limitations (fully preserved in Prohibited Conclusions), provenance (intact, citing exact URL/date), supersession/coexistence state (addressed directly in §5 below), fail-closed compatibility (not applicable in the sense a conditional claim requires, but confirmed by absence — no code path infers or defaults an Enterprise determination either way). Conversational reachability of `runway-gen3` was not independently re-verified by this review either — consistent with `CPR_016` §5's and Kling's `CPR_013` §12's own treatment of this as a separately-timed concern outside CPR scope, not a publication blocker. **No remaining blocker identified beyond the one this reconsideration exists to test (§5).**

## 5. Post-retirement empirical canary — the load-bearing finding of this reconsideration

Executed live, against the real current (retired) Matrix state, via a throwaway script (`runway-cpr017-postretirement-canary-throwaway.ts`, deleted immediately after use, never committed) mirroring `CPR_016`'s own canary construction exactly: the real `MATRIX_FIXTURE` (Runway `crc_eligible: 'No'`) supplied unmodified, alongside one ephemeral synthetic `TopicClaim` object (mechanically transcribed from the real `GOVERNED-CLAIMS.md` fields, `lifecycle: 'Adopted'`/`crc_eligible: 'Yes'` forced only inside this throwaway script, never written to any repository file), a real `commercial_use` `UserGoal`, and a real `runway-gen3` tool mention, run through the actual `retrieve()` and `buildBoundedInterpretations()` code paths unmodified.

Result:
- **`out.results.length === 1`** (not 2) — confirmed empirically, not inferred. `out.diagnostics === [{identifier: 'runway-gen3', reason: 'no_eligible_claims'}]` — the retired Matrix row correctly produces zero eligible claims and is silently excluded, exactly as `enumerateEligibleClaims`'s existing `crc_eligible === 'Yes'` filter is designed to do.
- **Matrix-origin result count: 0.** **Successor-origin result count: 1** (`claim_id: 'CLAIM-RUNWAY-COMMERCIAL-USE-001-v1'`).
- Fed through `buildBoundedInterpretations`: exactly one `BoundedInterpretation`, `status: 'directly_relevant'`, `supporting_claim_ids: ['CLAIM-RUNWAY-COMMERCIAL-USE-001-v1']`. Its `summary` is the successor's candidate statement verbatim, with the Enterprise exclusion sentence intact and unaltered: *"...This does not apply to Runway's Enterprise tier."* **No duplication, no scope inconsistency, no adjacent/contradicting statements** — the exact defect `CPR_016` §7 empirically proved is confirmed, empirically, to no longer occur.
- No downstream dedup participated in producing this clean result — there is nothing to deduplicate, since the Matrix side genuinely contributes zero results at the Retrieval stage itself (same structural finding as Kling's own post-retirement canary, `CPR_015` §G).
- **Unrelated-provider collateral check:** the same script separately ran a real `retrieve()` call for Kling against the real, published `TOPIC_CLAIMS_FIXTURE` — result count 1 (`CLAIM-KLING-COMMERCIAL-USE-BASELINE-001-v1`), matching Kling's already-established published state exactly. Runway's retirement had no collateral effect on Kling's independent publication.

## 6. Known test debt — reconfirmed non-blocking, not re-investigated from scratch

The prior new-failure diagnostic individually verified all 5 new `retrieve.test.ts` failures introduced by the retirement commit as stale fixture-choice issues (the tests use `runway-gen3` merely as a convenient "example unconditional Matrix `Yes` claim" for generic mechanics assertions — dedup, backward-compatible call shape, verbatim passthrough — none substantively about Runway), correctly distinguishing a sixth, superficially-similar failure (`test 7: multi-tool handoff`) as pre-existing and Kling-attributable, not newly introduced by this commit. This review re-ran one of the five (`test 2: one resolved tool + one Yes claim`) directly as a targeted sanity check rather than the full suite, and confirmed the identical failure mode: `Expected length: 1, Received length: 0` — exactly the stale hardcoded expectation the prior diagnostic identified, no contradiction found. **This is test-maintenance debt, clearly separated from publication-safety substance** — none of the 5 failures touch proposition text, evidence, applicability, dependencies, or (per §5's live canary, which exercises the real code paths these tests also exercise, just with different fixture inputs) actual coexistence/retrieval behavior. Not fixed here, per this task's explicit scope.

## 7. Fail-closed / architecture check

Publication would use only existing, unmodified generic Living Knowledge machinery — `TopicClaim`, `tool_scope`, `Lifecycle`, `crc_eligible`, confirmed via §5's live execution through the real, unmodified code paths. No Runway-specific Retrieval, BI, or Composition logic exists or would be needed; no provider-specific question orchestration; no fabricated `UserGoal`; no applicability inference (none is possible, since none is needed). The known "answered-but-unresolved applicability" CRC-engine gap (identified during Kling's paid-Starter UAT diagnostic) is **not reachable for this claim** — that gap concerns a governed `ApplicabilityFact` remaining unresolved after an imprecise user answer; `CLAIM-RUNWAY-COMMERCIAL-USE-001-v1` has `applicability_requirements: []`, so there is no applicability fact for that gap to ever apply to. Stated explicitly, not invoked reflexively by analogy to Kling.

## 8. What this review does NOT establish

Not a Commercial Assurance Assessment. Not legal advice. Not project clearance or certification. Not proof any particular project satisfies or avoids the competitive-products restriction (ToS §5) — that remains case-specific judgment this claim does not perform. Not a re-adjudication of `FGR_015`'s Adoption decision, which is unaffected. Not an alteration of `CPR_016`'s own historical record, which remains the accurate account of the pre-retirement WITHHOLD finding.

## 9. CLI recommendation

**APPROVE.**

`CPR_016`'s sole, sufficient blocker — live, non-vacuous Matrix coexistence producing a scope-inconsistent composed output — has been removed by an explicitly PM/JD-authorized, correctly-executed Matrix retirement (§0, §5), and this removal has been empirically re-verified against the real post-retirement code state, not assumed from the retirement commit's existence alone. Every other publication requirement independently passes on this reconsideration's own fresh checks, not inherited from either prior review: evidence is CURRENT (§2, third independent fetch), claim semantics are unchanged and unstrengthened (§3), no other CRC-eligibility blocker was found (§4), the post-retirement empirical canary confirms a single, clean, non-duplicative, non-contradictory result with the Enterprise exclusion intact (§5), known test debt is confirmed cosmetic and non-substantive (§6), and no architectural or provider-specific concern exists (§7). This is a genuine, independently-earned APPROVE, not an automatic conversion of `CPR_016`'s WITHHOLD.

**Proposed CRC Publication Scope / Candidate Statement:** the already-drafted text in `GOVERNED-CLAIMS.md` (§10 of `CPR_016`, reproduced there as available for direct reuse) remains accurate and is recommended for use without further drafting work: *"Under Runway's current Terms of Use, you may use generated Output commercially across Runway's Free, Standard, Pro, and Max tiers, subject to a restriction against using the Services or Outputs to create, train, develop, or improve similar or competitive products or services. This does not apply to Runway's Enterprise tier."*

This recommendation does **not** itself modify `GOVERNED-CLAIMS.md`'s CRC publication fields, publish any `topic-claims-fixture.ts` entry, or perform any runtime activation — those remain a separate, later, explicitly-authorized publication milestone, per every prior precedent in this line (Kling's `CPR_015` → separate publication task).

--- END VERBATIM CRC PUBLICATION REVIEW ---
