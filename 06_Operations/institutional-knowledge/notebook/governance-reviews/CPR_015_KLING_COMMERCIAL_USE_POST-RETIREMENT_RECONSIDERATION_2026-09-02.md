Title: CRC Publication Review #15 — Kling Commercial-Use Baseline + Member-Account Exception, Reconsideration Following Executed (Production-Confirmed) Matrix Retirement

Reviewed claims:
- `CLAIM-KLING-COMMERCIAL-USE-BASELINE-001-v1`
- `CLAIM-KLING-COMMERCIAL-USE-MEMBER-001-v1`

Review date: 2026-09-02

Artifact type: CRC Publication Review (publication stage) — a second reconsideration, superseding `CPR_014`'s "CONDITIONAL APPROVE — contingent on execution" (corrected by the `CPR_014` disposition-correction addendum to authoritative **WITHHOLD**, reconsideration condition narrowed to "execution of the authorized retirement"). That execution condition is now satisfied: the Matrix retirement commit (`43b78c0b430e8343a6eed152f1e2fef1e831059c`) is confirmed both on `origin/main` and, per human-supplied Vercel dashboard evidence (deployment `2UVg5me6GLvcJfLwTN7SgK9tSQEj`, source commit `43b78c0`, status Ready, environment Production, routing Current, domain `app.superimmersive8.com` assigned), live in production. This is the first Kling CPR conducted against genuinely *executed*, not merely *authorized*, Matrix retirement. Per the combined-review convention (`CPR_006`, extended by `CPR_007`/`CPR_013`), this uses `CPR_NNN_<PACKAGE_LABEL>_<review-date>.md` naming, continuing the independent CPR sequence at #15.

PM decision: **PENDING** — this artifact records a CLI/reviewing-agent recommendation for PM decision, mirroring `CPR_013`'s and `CPR_014`'s own self-authored-recommendation precedent within this same governance thread (as distinct from `FGR_013`'s and `FGR_014`'s PM decisions, both of which were recorded only after an explicit, verbatim human decision statement preceded the artifact's writing — no such statement precedes this artifact). This review's own recommendation is below (§6).

Historical status: VERBATIM ARCHIVE — DO NOT EDIT HISTORICAL BODY once PM's decision is recorded. Future amendments should be appended outside the body below, or captured in a new review artifact — never inserted into the verbatim body.

Source: written directly to this file as the review was conducted this session.

--- BEGIN VERBATIM CRC PUBLICATION REVIEW ---

# Kling Commercial-Use — CPR Reconsideration Following Executed, Production-Confirmed Matrix Retirement

## 0. Repository / governance state verified before this review

LK worktree (`work/lk-eu-ai-act-art50-research`) confirmed `HEAD == origin/main == 43b78c0b430e8343a6eed152f1e2fef1e831059c`, clean, before this review began. `origin/main` fetched fresh and found unchanged from the retirement commit — no intervening commit exists that touches Matrix, TopicClaims, Retrieval, applicability, askability, Bounded Interpretation, Composition, or publication policy. (A sibling branch, `work/crc-consultative-composition-docs-closeout`, carries a documentation-only commit built on top of `43b78c0` — confirmed to remain a separate, unmerged branch, not on `main`, and irrelevant to this review's surface.) Authoritative material re-read directly this review, not inherited from prior summaries: `FGR_013`, `CPR_013`, `FGR_014`, `CPR_014`, the `CPR_014` disposition-correction addendum, `GOVERNED-CLAIMS.md`'s current Kling entries, `PLATFORM-RIGHTS-MATRIX.md`'s Kling section, `matrix-fixture.ts`'s Kling row, `topic-claims-fixture.ts` (confirmed zero Kling entries), and `CRC-PUBLICATION-POLICY.md` (confirmed fresh: the tool-scoped coexistence-check practice names exactly APPROVE or WITHHOLD as its recorded disposition — no third value for this specific check).

## 1. What changed since CPR_014 / the addendum

`CPR_014`'s own crux finding (§3) was that an unconditional standing `Yes` could not be granted while the Matrix claims remained live, because `crc_eligible: 'Yes'` was still their real, current value at that time. **That precondition no longer holds.** Direct inspection of the actual committed files, this review, confirms both `kling-commercial-use-baseline` and `kling-commercial-use-member` now read `crc_eligible: 'No'` in both `PLATFORM-RIGHTS-MATRIX.md` and `matrix-fixture.ts`, each with a dated retirement annotation citing `FGR_014` and the corresponding successor TopicClaim ID — exactly the mechanical encoding `FGR_014` §4 specified, correctly executed. This is not merely a repository-state fact: the human-supplied Production deployment evidence (see artifact header) confirms this is also the *live, currently-serving* state at `app.superimmersive8.com`, not only a committed-but-undeployed change.

## 2. Evidence freshness — independently re-checked

Per `CRC-PUBLICATION-POLICY.md` Principle 1 ("never inferred from Status") and the addendum's own explicit instruction not to treat a same-day "zero elapsed time" note as continuing evidence of freshness once material time has passed: this review conducted a fresh, bounded check. A live web search for Kling AI Terms of Service / Terms of Paid Service changes since 2026-04-21 returned only convergent secondary sources (an AI-tooling pricing/legal-guide blog, a WaveSpeed API pricing post) restating the same substance already reflected in K1/K2 (paid/Member commercial-use permission, free-tier non-commercial restriction) — no source indicates any change to the reviewed provisions since capture. One incidental corroboration is notable: an independent secondary source states "Kling AI members' use of the Output for commercial purposes is not restricted... provided you have an active paid subscription (Standard, Pro, or Premier)" — language that itself risks exactly the plan-tier/membership-status conflation K2 §4.3.3 and this claim's own Prohibited Conclusions warn against (see §4 below); it is treated here as external corroborating evidence of substance-stability, not as evidence for a broader proposition than K1/K2 themselves support. **Finding: no material change found. Freshness remains Classification A** — now independently re-verified at this reconsideration's own review time (2026-09-02), not merely inherited from `CPR_013`'s or `CPR_014`'s same-day findings.

## 3. Applicability / dependency re-verification

Re-confirmed by direct code inspection, not inherited: `APPLICABILITY_FACTS` (`retrieval-engine/types.ts`) is `['jurisdiction', 'tool_plan_tier', 'tool_account_status']` — `tool_account_status` remains first-class, unchanged. `selector-askability.ts` confirms `tool_account_status` remains registered `askable_in_crc`, unchanged, same single fixed generic question template. The member claim's `applicability_requirements: [{ fact: 'tool_account_status', tool: 'kling', operator: 'equals', value: 'Member Account' }]` is unchanged from `FGR_013`. Both claims' `unresolved_project_dependencies: []` remains correct — nothing invented, nothing added. No code path anywhere derives `tool_account_status` from `tool_plan_tier`; the two remain structurally independent, exactly as found by the earlier production-UAT diagnostic (this session) that traced this distinction for real production input.

## 4. Post-retirement coexistence re-verification — the deciding check

This worktree has no installed `node_modules` (confirmed: `08_Platform/app/node_modules` absent) — a pre-existing, previously disclosed limitation, unchanged since `CPR_013`/`CPR_014`'s own throwaway-script canaries were run in a different, dependency-installed context. **This review's coexistence verification is therefore analytical/code-path verification via direct reading of the exact same production functions `CPR_013`'s empirical canary exercised, not a live `tsx`/Jest execution.** Disclosed plainly, not silently substituted, per this thread's established honesty discipline (matching the addendum's own disclosure norm for its freshness-check limitation).

The trace, read directly from the current committed source:

- `enumerate-eligible-claims.ts`: `enumerateEligibleClaims(row)` returns `row.claims.filter((c) => c.crc_eligible === 'Yes')` — a single-line, unconditional, pure filter with no branching. Applied to the current Kling `MatrixRow` (both claims now `'No'`), this returns `[]`.
- `retrieve.ts` lines 144-148: `const eligibleClaims = enumerateEligibleClaims(row); if (eligibleClaims.length === 0) { diagnostics.push({ identifier, reason: 'no_eligible_claims' }); continue }` — confirmed directly: when a matched row's eligible-claims array is empty, the Matrix path contributes **zero** `RetrievalResult` entries for that row, records an explicit `no_eligible_claims` diagnostic, and moves on. No other code path in `retrieve.ts` can resurrect a `'No'`-eligible Matrix claim.
- `lookup-topic-claims.ts`: `lookupTopicClaims()` operates purely over its own `topicClaims: TopicClaim[]` parameter (in production, `TOPIC_CLAIMS_FIXTURE`; in a hypothetical post-publication state, the two Kling entries) — confirmed by signature and body inspection to have zero read access to `MatrixRow`/`MatrixClaim` state. This origin is, and always was, structurally independent of Matrix state (this was already true when Matrix was live — see `CPR_013` §14's non-colliding-dedup-key finding); what has changed is not this independence itself but the Matrix side's *output*.

**Conclusion, deterministically implied by this trace, not merely inferred:** with both Kling Matrix claims now `crc_eligible: 'No'` in the real, currently-deployed fixture, the Matrix path produces **zero** results for Kling under any input state — there is no longer a second origin for any TopicClaim-origin result to duplicate against. This is a stronger, more decisive finding than `CPR_013`'s own coexistence analysis established for the live-Matrix case (which only showed non-colliding dedup *keys* while both origins were simultaneously live and both produced output) — retirement did not just avoid a naming collision, it eliminated one origin's output entirely. No explicit/discovered provenance concern arises, since the only origin capable of producing a Kling commercial-use result today (if the TopicClaims were published) would be the TopicClaim path, tagged `matrix_identifier: claim.topic` (`'commercial_use'`), exactly as `assemble-result.ts` already assigns for every other TopicClaim-origin result in this corpus.

**Recommended operational note, not a precondition of this review's disposition:** because this verification is analytical rather than live-executed, the future publication milestone (which will install dependencies to add the fixture entries and run the existing test suite) should include a live re-run of `runSyntheticEligibilityCanary` or an equivalent direct pipeline call, post-fixture-addition, as a final confirming check — cheap to obtain once that task's environment has `node_modules`, and consistent with `CPR_014` §5.4's own instruction that a post-execution empirical canary is still warranted. This does not block this review's disposition, which rests on a deterministic, single-branch, side-effect-free function with no meaningful risk of divergence between static reading and live execution.

## 5. Fail-closed / Bounded Interpretation re-verification

Unchanged from `CPR_013` §14, §16 (empirically established there, not re-executed here since the underlying evaluator — `evaluateApplicabilityDetailed` / `lookupTopicClaims`'s three-state `unresolved`/`not_met`/`met` logic — is confirmed unmodified by direct code inspection this review): unknown `tool_account_status` → member claim withheld, fails closed, never guessed; confirmed non-member (`not_met`) → withheld, does not affirmatively assert ineligibility; confirmed Member Account → `directly_relevant`, candidate statement rendered verbatim including its competing-products carve-out. Baseline claim unconditional, applies identically regardless of account status. No layer asserts a conclusion stronger than the governed candidate statement text; CRC output remains educational, not assurance/certification, per `CPR_013` §16-§18's own findings, unaffected by the retirement.

## 6. Disposition

**APPROVE — for both `CLAIM-KLING-COMMERCIAL-USE-BASELINE-001-v1` and `CLAIM-KLING-COMMERCIAL-USE-MEMBER-001-v1`.**

Decided independently, arriving at the same result for both: freshness adequate (§2), applicability/dependency handling correct and unchanged (§3), the sole blocker that produced `CPR_013`'s and `CPR_014`'s WITHHOLD — Matrix coexistence — is now structurally eliminated by executed, production-confirmed retirement (§4), and fail-closed behavior remains intact (§5). This APPROVE is valid under the present, real, production-confirmed state — not conditional on any future action. Per `CRC-PUBLICATION-POLICY.md`'s coexistence-check step 4 ("If compatibility cannot be established, WITHHOLD... until it can"): compatibility has now been established, on the record, by the retirement's actual execution — this is the direct, literal satisfaction of that trigger's own converse, not a policy exception or a weakening.

This disposition uses only the repository's established CPR vocabulary (APPROVE / WITHHOLD / ESCALATE) — no new disposition shape is introduced, correcting rather than repeating `CPR_014`'s original vocabulary error.

**This APPROVE authorizes CRC eligibility in substance. It does NOT itself authorize runtime `TOPIC_CLAIMS_FIXTURE` publication, and it does NOT itself update `GOVERNED-CLAIMS.md`'s `CRC Eligible`/`CRC Approver`/`CRC Decision Date` fields** — both remain a separate, later, explicitly-authorized milestone, per this task's own instruction not to silently combine governance approval and implementation, and per this review's own PM-decision-PENDING status (§ header) mirroring `CPR_013`/`CPR_014`'s self-authored-recommendation precedent within this same thread. `GOVERNED-CLAIMS.md` is unmodified by this artifact; both claims' ledger entries continue to read `CRC Approver: PENDING`, `CRC Decision Date: PENDING`, now stale relative to this review's own recommendation but not silently or presumptively updated ahead of an explicit PM decision.

## 7. What the eventual publication milestone should do, once PM concurrence is given

Not performed here; recorded for operational continuity only, consistent with `CPR_014` §5's own restatement discipline:
1. Confirm this artifact (`CPR_015`) is still the governing recommendation and that no intervening Matrix or governance change has occurred since.
2. Obtain explicit PM/JD concurrence with this review's APPROVE recommendation (a distinct step from this artifact's own authorship, per this thread's established practice).
3. Update `GOVERNED-CLAIMS.md`: set `CRC Approver`, `CRC Decision Date` on both claims, and promote the already-drafted `CRC Candidate Statement` text (verbatim, both claims, unchanged since `CPR_013`'s own fidelity check — reconfirmed faithful, not re-drafted, by this review) into `CRC Publication Scope`.
4. Add both claims to `TOPIC_CLAIMS_FIXTURE`, in the same or an immediately subsequent commit.
5. Run the existing consistency/regression test suite (once `node_modules` is available in whatever environment performs this step) plus a live post-publication synthetic-eligibility-canary run, as the empirical confirmation this review's analytical trace (§4) predicts but does not itself constitute.

## 8. What this review does NOT establish

Not a Commercial Assurance Assessment. Not legal advice. Not project clearance or certification. Not itself a `GOVERNED-CLAIMS.md` update or runtime publication (§6). Not a finding that `CPR_013`'s or `CPR_014`'s WITHHOLD dispositions were wrong at the time they were made — both were correct on the world as it then stood; this review reflects a subsequent, distinct governance event (executed, production-confirmed Matrix retirement), not a correction of either prior review. Not an empirically-executed coexistence test (§4's disclosed limitation) — an analytical one, of a kind this review holds to be adequate given the deterministic, single-branch nature of the code traced, but explicitly not represented as equivalent in evidentiary form to `CPR_013`'s own live-executed canary.

--- END VERBATIM CRC PUBLICATION REVIEW ---
